# 粮仓温度三维数组接口文档

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 根据粮房编号获取每个采集时间点的三维温度数组 |
| 请求方式 | `GET` |
| 接口路径 | `/data/temperatureCube` |
| 鉴权要求 | **无需鉴权**（已加入白名单，直接访问） |
| 返回格式 | `application/json; charset=utf-8` |

> 说明：该接口已配置为公开接口（`SecurityConfiguration.writeList` 中 `permitAll`），调用时**不需要**携带 `Authorization` 头。

## 2. 请求参数

### 2.1 Query 参数

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `houseNo` | String | 是 | 粮房编号（`house` 表的 `HouseNo` 字段） |

### 2.2 请求示例

```
GET http://{服务器地址}:8080/data/temperatureCube?houseNo=HH001
```

使用 curl：

```bash
curl "http://localhost:8080/data/temperatureCube?houseNo=HH001"
```

## 3. 响应说明

### 3.1 统一响应结构

所有接口统一使用 `R` 包装返回：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | String | 状态码，`200` 表示成功，`500` 表示失败 |
| `message` | String | 提示信息，成功为 `success`，失败为 `error` |
| `show` | String | 前端是否弹窗提示，`0` 表示不弹窗 |
| `value` | Object | 业务数据（见下方说明） |

### 3.2 value 结构

`value` 是一个对象（Map）：

- **key**：采集时间 `TestDate`，格式为 ISO-8601 字符串，如 `2026-09-01T10:15:30`，按时间升序排列（`LinkedHashMap` 保持有序）；
- **value**：该时间点对应的三维温度数组 `[层][行][列]`，即 `[z][x][y]`，每层是一个「行 × 列」的二维数组。

其中维度取值来自 `house` 表的仓房配置：

| house 字段 | 含义 | 维度 |
| --- | --- | --- |
| `z_num`（`z`） | 层数 | 第一维（层） |
| `x_num`（`x`） | 行数 | 第二维（行） |
| `y_num`（`y`） | 列数 | 第三维（列） |

数组元素为**字符串**类型的温度值，统一保留 1 位小数（如 `"25.0"`）。

### 3.3 响应示例

```json
{
  "code": "200",
  "message": "success",
  "show": "0",
  "value": {
    "2026-09-01T10:15:30": [
      [
        ["25.0", "25.1", "24.9"],
        ["25.3", "25.2", "25.0"]
      ],
      [
        ["24.8", "24.9", "24.7"],
        ["25.1", "25.0", "24.9"]
      ]
    ],
    "2026-09-01T14:30:00": [
      [
        ["26.0", "26.1", "25.9"],
        ["26.3", "26.2", "26.0"]
      ],
      [
        ["25.8", "25.9", "25.7"],
        ["26.1", "26.0", "25.9"]
      ]
    ]
  }
}
```

> 示例中仓房配置为 `z=2`（2 层）、`x=2`（2 行）、`y=3`（3 列），因此每个时间点返回 2 层，每层为 2 行 × 3 列。

### 3.4 边界情况

| 场景 | 返回值 |
| --- | --- |
| 粮房编号不存在（`house` 表中查不到） | `value` 为空对象 `{}` |
| 粮房存在但无温度数据 | `value` 为空对象 `{}` |
| 单个数据记录 `gasStrength` 为 null | 该记录被跳过 |
| 温度值为 `NULL` 字符串 | 解析为 `"0"` |
| 某点位缺失（源数据未覆盖） | 该点位返回 `"-999"`（前端需转数字并识别为无效温度） |

## 4. 源数据格式说明（供参考）

`receiver_data` 表的 `gasStrength` 字段保存原始温度数据，解析规则如下：

- 以 `#` 分割多条记录；
- 每条记录形如 `列:行:层$温度`，以 `$` 分割坐标与温度；
- 坐标为 **1 起始**（从 1 开始计数），解析时自动减 1 转为数组下标；
- 例：`3:2:1$25.0#2:2:1$24.8#...` 表示第 1 层（2 行、3 列）中的两个点位温度。

## 5. 错误码

| code | 含义 | 说明 |
| --- | --- | --- |
| `200` | 成功 | 正常返回 |
| `500` | 服务端错误 | 处理异常时返回，`message` 为 `error` |

> 由于该接口为公开接口（无需鉴权），正常不会返回 `401` / `403`。
