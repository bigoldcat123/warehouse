# 粮温数据 API 文档

## 基础信息

- **基础路径**: `/data`
- **请求协议**: HTTP/HTTPS
- **数据格式**: JSON

---

## 1. 查询指定点温度记录

根据粮房编号和三维坐标（层、行、列），返回该点在历史各时间点的温度记录。

### 请求

```
GET /data/tempRecords
```

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| houseNo | String | 是 | 粮房编号 |
| ceng | int | 是 | 层坐标（从1开始） |
| hang | int | 是 | 行坐标（从1开始） |
| lie | int | 是 | 列坐标（从1开始） |

### 请求示例

```bash
curl -X GET "http://localhost:8080/data/tempRecords?houseNo=H001&ceng=1&hang=2&lie=3"
```

### 响应示例

```json
{
  "code": 200,
  "msg": "success",
  "data": [
    {
      "testDate": "2026-08-06 08:00:00",
      "temp": 16.7
    },
    {
      "testDate": "2026-08-06 12:00:00",
      "temp": 18.3
    },
    {
      "testDate": "2026-08-06 18:00:00",
      "temp": 17.5
    }
  ]
}
```

### 响应字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| testDate | String | 检测时间，格式：yyyy-MM-dd HH:mm:ss |
| temp | Float | 该点的温度值（摄氏度） |

---

## 2. 查询指定层平均温度

根据粮房编号和层坐标，返回该层所有测温点在每个时间点的平均温度。

### 请求

```
GET /data/layerAvgTemp
```

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| houseNo | String | 是 | 粮房编号 |
| ceng | int | 是 | 层坐标（从1开始） |

### 请求示例

```bash
curl -X GET "http://localhost:8080/data/layerAvgTemp?houseNo=H001&ceng=1"
```

### 响应示例

```json
{
  "code": 200,
  "msg": "success",
  "data": [
    {
      "testDate": "2026-08-06 08:00:00",
      "temp": 15.42
    },
    {
      "testDate": "2026-08-06 12:00:00",
      "temp": 17.18
    },
    {
      "testDate": "2026-08-06 18:00:00",
      "temp": 16.35
    }
  ]
}
```

### 响应字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| testDate | String | 检测时间，格式：yyyy-MM-dd HH:mm:ss |
| temp | Float | 该层所有点的平均温度（摄氏度） |

### 计算逻辑

对于每个时间点，提取指定层的所有测温点温度，计算算术平均值：

```
avg = (temp1 + temp2 + ... + tempN) / N
```

其中 N 为该层的测温点总数。

---

## 3. 查询全部点平均温度

根据粮房编号，返回该粮房所有测温点在每个时间点的平均温度。

### 请求

```
GET /data/allAvgTemp
```

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| houseNo | String | 是 | 粮房编号 |

### 请求示例

```bash
curl -X GET "http://localhost:8080/data/allAvgTemp?houseNo=H001"
```

### 响应示例

```json
{
  "code": 200,
  "msg": "success",
  "data": [
    {
      "testDate": "2026-08-06 08:00:00",
      "temp": 14.85
    },
    {
      "testDate": "2026-08-06 12:00:00",
      "temp": 16.42
    },
    {
      "testDate": "2026-08-06 18:00:00",
      "temp": 15.68
    }
  ]
}
```

### 响应字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| testDate | String | 检测时间，格式：yyyy-MM-dd HH:mm:ss |
| temp | Float | 全部测温点的平均温度（摄氏度） |

### 计算逻辑

对于每个时间点，提取该粮房的所有测温点温度，计算算术平均值：

```
avg = (temp1 + temp2 + ... + tempN) / N
```

其中 N 为该粮房的总测温点数。

---

## 错误响应

### 参数缺失

```json
{
  "code": 400,
  "msg": "Required parameter 'houseNo' is missing",
  "data": null
}
```

### 粮房不存在

```json
{
  "code": 200,
  "msg": "success",
  "data": []
}
```

**注意**: 如果粮房不存在或无数据，返回空数组 `[]`，不会报错。

---

## 数据格式说明

### 温度数据原始格式

系统内部温度数据以字符串形式存储，格式为：

```
列:行:层$温度值$5#列:行:层$温度值$5#...
```

**示例**:
```
1:1:1$16.7$5#1:2:1$15.3$5#2:1:1$14.2$5#1:1:2$12.1$5
```

**解析规则**:
- `#` 分隔不同测温点
- `$` 分隔坐标、温度值和其他信息
- `:` 分隔三维坐标（列:行:层）
- 坐标从 1 开始计数
- 温度值单位为摄氏度（℃）
- `NULL` 表示无效温度，按 0 处理

---

## 使用场景

### 场景1: 监控特定位置温度变化趋势

使用 `/data/tempRecords` 接口，查询某个关键位置（如粮堆中心）的温度历史，用于判断是否存在局部发热风险。

### 场景2: 分析某层整体温度分布

使用 `/data/layerAvgTemp` 接口，查询某层的平均温度，对比不同层之间的温度差异，评估通风效果。

### 场景3: 粮房整体温度监控

使用 `/data/allAvgTemp` 接口，查询整个粮房的平均温度，用于宏观监控和预警。

---

## 注意事项

1. **坐标系统**: 所有坐标参数从 1 开始，不是从 0 开始
2. **时间格式**: 返回的时间格式为 `yyyy-MM-dd HH:mm:ss`
3. **温度精度**: 温度值保留一位小数
4. **空值处理**: 原始数据中的 `NULL` 温度值会被视为 0℃ 参与计算
5. **数据过滤**: 如果某个时间点没有有效温度数据，该时间点不会出现在返回结果中

---

## 版本历史

- **v1.0** (2026-08-06): 初始版本，包含三个温度查询接口
